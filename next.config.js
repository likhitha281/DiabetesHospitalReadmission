/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // If you need to process CSV files or use Python outputs
  // You might need to add API routes or use static exports
}

module.exports = nextConfig

